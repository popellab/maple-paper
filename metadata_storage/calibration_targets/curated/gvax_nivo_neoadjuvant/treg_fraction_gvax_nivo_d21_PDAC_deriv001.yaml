study_interpretation: |
  Li et al. (Cancer Cell 2022) performed multi-omic profiling of tumor specimens from a platform trial in resectable PDAC in which patients received neoadjuvant GVAX alone (arm A) or GVAX plus nivolumab (arm B) followed by surgical resection. Multiplex IHC was used to quantify CD4+ and CD8+ T cell subsets, including FoxP3+ regulatory T cells (Tregs), in intratumoral tertiary lymphoid aggregate (LA) regions and non-LA tumor areas. Figure 2A reports post-treatment densities (percentage of all cells in the region of interest) of multiple immune cell subtypes in LAs for arm A and arm B. We extract the mean and standard deviation of the fraction of FoxP3+ Tregs among all cells in post-treatment LAs in arm B as a proxy for the regulatory T cell fraction in the tumor after neoadjuvant GVAX + nivolumab.

key_assumptions:
- |
  The visually estimated Treg mean (0.08) and SD (0.04) from Figure 2A reasonably approximate the true values; uncertainty from manual figure reading is smaller than biological between-patient variability.
- |
  Percentages reported as 'density of immune cell subtypes' in Figure 2A represent fractions of all nucleated cells in the mIHC regions of interest within intratumoral lymphoid aggregates, and these fractions are treated as dimensionless values on [0,1].
- |
  The experimental measurement (FoxP3+ Tregs as percentage of all cells in LA ROIs) is approximated by the fraction of Tregs among all modeled immune cells in V_T. This is a better match than T-cells-only because LAs contain macrophages, DCs, and other myeloid cells alongside lymphocytes. The main unmodeled component is B cells, which constitute 50-70% of tertiary lymphoid structure cellularity; their absence from the denominator makes the model prediction systematically higher than the experimental value by an estimated 2-3x.
- |
  A beta distribution is an appropriate approximation for the sampling distribution of a bounded fraction (Treg proportion), using parameters inferred from the mean and SD extracted from the figure.

key_study_limitations:
- |
  Treg fractions and other immune cell densities are estimated from figure panels rather than numeric tables, introducing additional measurement uncertainty beyond the reported SDs.
- |
  The mIHC regions of interest focus on intratumoral lymphoid aggregates and may not capture the full spatial heterogeneity of Tregs across the entire primary tumor mass.
- |
  The clinical trial sample size is small (n=10 for arm B with evaluable LAs in Figure 2A), so estimates of between-patient variability for Treg fractions are imprecise.
- |
  B cells, which constitute 50-70% of cells in tertiary lymphoid structures, are not modeled. Their absence from the observable denominator means the model-predicted Treg fraction will be systematically higher than the experimental value by an estimated 2-3x. This systematic bias should be considered when interpreting calibration fit.

observable:
  code: |
    def compute_observable(time, species_dict, constants, ureg):
        # Fraction of FoxP3+ Tregs among all modeled immune cells in V_T.
        # Approximates the experimental measurement: Tregs as % of all cells
        # in lymphoid aggregate ROIs (Li 2022, Figure 2A).
        # B cells (major LA component, 50-70%) are not modeled, so the
        # denominator is smaller than reality — see key_assumptions.
        treg = species_dict['V_T.Treg']
        cd8 = species_dict['V_T.CD8']
        th = species_dict['V_T.Th']
        cd8_exh = species_dict['V_T.CD8_exh']
        th_exh = species_dict['V_T.Th_exh']
        mac_m1 = species_dict['V_T.Mac_M1']
        mac_m2 = species_dict['V_T.Mac_M2']
        nk = species_dict['V_T.NK']
        cdc1 = species_dict['V_T.cDC1']
        cdc2 = species_dict['V_T.cDC2']
        mcdc1 = species_dict['V_T.mcDC1']
        mcdc2 = species_dict['V_T.mcDC2']
        mdsc = species_dict['V_T.MDSC']

        total_immune = (treg + cd8 + th + cd8_exh + th_exh
                        + mac_m1 + mac_m2 + nk
                        + cdc1 + cdc2 + mcdc1 + mcdc2 + mdsc)
        frac = (treg / total_immune).to('dimensionless')
        return frac
  units: dimensionless
  species:
  - V_T.Treg
  - V_T.CD8
  - V_T.Th
  - V_T.CD8_exh
  - V_T.Th_exh
  - V_T.Mac_M1
  - V_T.Mac_M2
  - V_T.NK
  - V_T.cDC1
  - V_T.cDC2
  - V_T.mcDC1
  - V_T.mcDC2
  - V_T.MDSC
  constants: []
  inputs: []
  support: unit_interval
  mapping_rationale: |
    Multiplex IHC in Li et al. (Cancer Cell 2022) quantifies FoxP3+ CD4+ Tregs as a percentage of all cells in ROIs within intratumoral lymphoid aggregates (LAs) after neoadjuvant GVAX + nivolumab. The experimental denominator includes all nucleated cells in the ROI (T cells, B cells, macrophages, DCs, myeloid cells, and some stromal/tumor cells). To approximate this, the model observable uses Tregs divided by all modeled immune cells in V_T: T cell subsets (CD8, Th, Treg, and exhausted variants), macrophages (M1, M2), NK cells, DCs (cDC1, cDC2, mcDC1, mcDC2), and MDSCs. The principal unmodeled component is B cells, which are abundant in LAs (50-70% of cells in tertiary lymphoid structures). Their absence makes the model denominator smaller than the true experimental denominator, so the predicted Treg fraction will be systematically higher than the measured value.
  experimental_denominator: all cells in ROI within intratumoral lymphoid aggregates (all nucleated cells including T cells, B cells, macrophages, DCs, myeloid cells, stromal cells)
  model_denominator_species:
  - V_T.Treg
  - V_T.CD8
  - V_T.Th
  - V_T.CD8_exh
  - V_T.Th_exh
  - V_T.Mac_M1
  - V_T.Mac_M2
  - V_T.NK
  - V_T.cDC1
  - V_T.cDC2
  - V_T.mcDC1
  - V_T.mcDC2
  - V_T.MDSC
  unmodeled_denominator_components: |
    B cells (50-70% of cells in tertiary lymphoid structures) are not modeled. Their absence makes the model denominator smaller than the true experimental denominator, so the predicted Treg fraction will be systematically ~2-3x higher than the measured value. Non-immune cells (endothelial, stromal) in LAs are also unmodeled but are a minor fraction.
  aggregation:

experimental_context:
  species: human
  system: clinical.resection
  mouse_subspecifier:
  indication: PDAC
  treatment:
    history:
    - treatment_naive
    status: on_treatment
    specifier: Neoadjuvant GVAX vaccine plus nivolumab (arm B) prior to surgical resection.
  stage:
    extent: resectable
    burden: moderate
  cell_lines:
  culture_conditions:
  tissue_source: |
    Resected primary pancreatic ductal adenocarcinoma after neoadjuvant GVAX ± nivolumab.
  assay_type: |
    Multiplex immunohistochemistry (mIHC) with sequential staining and stripping in FFPE sections.

scenario:
  description: |
    Resectable PDAC patients randomized to neoadjuvant GVAX alone (arm A) or GVAX plus nivolumab (arm B) followed by surgical resection approximately 2 weeks after one treatment; multiplex IHC performed on post-treatment resection specimens to quantify immune cell subsets in tumor lymphoid aggregates (LAs) and non-LA tumor areas.
  interventions:
  - intervention_description: |
      Neoadjuvant GVAX (GM-CSF–secreting allogeneic pancreatic tumor cell vaccine) plus nivolumab (anti–PD-1 antibody) administered as per trial protocol in arm B, followed by surgical resection 2 weeks after initiation of therapy.

empirical_data:
  median:
  - 0.07442
  ci95:
  - - 0.02092
    - 0.17456
  units: dimensionless
  sample_size: 10
  sample_size_rationale: |
    Arm B (GVAX + nivolumab) post-treatment resection specimens with evaluable multiplex IHC in lymphoid aggregates; Figure 2A caption states 'arm A (n =9) versus arm B (n=10) patients.'
  index_values:
  index_unit:
  index_type:
  inputs:
  - name: treg_fraction_mean
    value: 0.08
    units: dimensionless
    description: |
      Estimated mean fraction of FoxP3+ regulatory T cells among all cells in post-treatment lymphoid aggregates (LAs) in arm B (GVAX + nivolumab), read manually from Figure 2A (Treg bar, red squares).
    source_ref: Li2022_CancerCell
    value_location: Figure 2A (Treg, LA, arm B)
    value_snippet: |
      Figure 2. Multiplex IHC of CD8+ or CD4+ T subtypes and their correlation with OS. (A and B) Summary of the density of all immune cell subtypes analyzed as indicated in LA (A) and non-LA tumor areas (B) in arm A (n =9) versus arm B (n=10) patients.
    input_type: direct_parameter
    conversion_formula:
    source_type: figure
    figure_id: Figure 2A
    extraction_method: manual
    extraction_notes: |
      Mean height of the Treg bar for arm B (GVAX+Nivo, red) in post-treatment LAs visually estimated from the y-axis (% of all cells in ROI) and converted to a fraction (0.08).
    dispersion_type:
    dispersion_type_rationale:
  - name: treg_fraction_sd
    value: 0.04
    units: dimensionless
    description: |
      Estimated standard deviation of FoxP3+ regulatory T cell fraction among all cells in post-treatment LAs in arm B (GVAX + nivolumab), read from the length of the error bar around the Treg mean in Figure 2A.
    source_ref: Li2022_CancerCell
    value_location: Figure 2A (Treg, LA, arm B)
    value_snippet: |
      All data shown as the mean ± SD; all comparisons by t test; *p < 0.05; **p < 0.01; ***p < 0.001; NS, not significant; all others, not significant.
    input_type: direct_parameter
    conversion_formula:
    source_type: figure
    figure_id: Figure 2A
    extraction_method: manual
    extraction_notes: |
      Standard deviation approximated visually from the size of the error bars for the Treg bar in arm B relative to the y-axis grid, then converted from percentage to fraction (0.04).
    dispersion_type: sd
    dispersion_type_rationale: |
      Figure 2 caption explicitly states 'All data shown as the mean ± SD', so the error bars are interpreted as standard deviations, not standard errors.
  assumptions:
  - name: n_mc_samples
    value: 10000.0
    units: dimensionless
    description: |
      Number of Monte Carlo samples used to approximate the sampling distribution of the Treg fraction.
    rationale: |
      10,000 beta-distribution draws provide stable estimates of the median and 95% credible interval while keeping computation inexpensive.
  distribution_code: |
    def derive_distribution(inputs, ureg):
        import numpy as np
        rng = np.random.default_rng(42)

        mean = inputs['treg_fraction_mean']  # dimensionless fraction
        sd = inputs['treg_fraction_sd']      # dimensionless fraction
        n_mc = int(inputs['n_mc_samples'].magnitude)

        m = mean.magnitude
        s = sd.magnitude
        var = s ** 2

        # Convert mean and variance on [0,1] to beta distribution parameters
        # mean = a / (a + b); var = a*b / ((a + b)**2 * (a + b + 1))
        k = m * (1.0 - m) / var - 1.0
        alpha = m * k
        beta = (1.0 - m) * k

        samples = rng.beta(alpha, beta, n_mc) * mean.units

        median_obs = np.median(samples)
        ci95_lower = np.percentile(samples, 2.5)
        ci95_upper = np.percentile(samples, 97.5)

        return {
            'median_obs': median_obs,
            'ci95_lower': ci95_lower,
            'ci95_upper': ci95_upper,
        }

primary_data_source:
  source_tag: Li2022_CancerCell
  title: |
    Multi-omic analyses of changes in the tumor microenvironment of pancreatic adenocarcinoma following neoadjuvant treatment with anti-PD-1 therapy
  first_author: Li
  year: 2022
  doi: 10.1016/j.ccell.2022.10.001

secondary_data_sources: []

calibration_target_id: treg_fraction_gvax_nivo_d21

cancer_type: PDAC

tags:
- ai-generated

logfire_trace_id: 019c683eb509d34931aa8144efd57c5c

extraction_model: gpt-5.1

extraction_reasoning_effort: medium

source_relevance:

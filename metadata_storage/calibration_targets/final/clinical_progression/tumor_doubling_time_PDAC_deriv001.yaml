study_interpretation: |
  Ahn et al. retrospectively analyzed 100 patients with histologically proven pancreatic ductal adenocarcinoma who underwent at least two contrast-enhanced multidetector CT scans with no intervening treatment. Tumor diameters and volumes were measured at each CT, and tumor growth rates and tumor volume doubling times (VDTs) were calculated. The reported VDTs (range 20.0 to 976.8 days, mean 132.3 +/- 132.1 days) represent macroscopic primary-tumor growth in situ in the pancreas prior to systemic therapy. For calibration, we interpret these VDTs as reflecting the effective net exponential growth rate of the primary PDAC mass in the tumor.primary compartment, and map them to model predictions by assuming that the number of cancer cells V_T.C1 is proportional to CT-visible tumor volume over the observation window.

key_assumptions:
- |
  Tumor cell number V_T.C1 is approximately proportional to CT-derived tumor volume over the observation interval, so that exponential growth rates and doubling times for cell count and volume are equivalent.
- |
  The reported 'mean, 132.3 +/- 132.1 days' for VDT represents mean +/- SD rather than SEM, based on the sample size (n=100) and consistency with the reported VDT range (20.0-976.8 days).
- |
  Tumor growth over the pre-treatment CT interval can be reasonably approximated as exponential for the purpose of defining a single VDT per patient, despite potential deviations from pure exponential growth.
- |
  The distribution of VDTs across these 100 largely advanced, untreated PDACs can be approximated by a lognormal distribution parameterized by the reported mean and SD on the linear scale, and correlations between VDT and initial tumor size are ignored for this calibration target.

key_study_limitations:
- |
  Single-center retrospective cohort of 100 patients from Korean institutions; external generalizability to broader PDAC populations may be limited.
- |
  The cohort includes a spectrum of tumor sizes (1.0-6.2 cm, mean 2.9 +/- 1.3 cm in diameter) and likely includes unresectable and metastatic disease, rather than being restricted to small, strictly resectable PDAC, so VDTs may differ somewhat from those of screen-detected resectable tumors.
- |
  VDTs were estimated from a limited number of CT timepoints per patient and assume exponential growth between scans; non-exponential growth patterns and measurement error in volumetry are not explicitly modeled.
- |
  The study does not stratify VDTs by resectability status or detailed clinical stage, limiting direct stage-specific calibration.

observable:
  code: |
    def compute_observable(time, species_dict, constants, ureg):
        import numpy as np
        # Compute a single overall tumor volume doubling time from the
        # first and last simulation timepoints, matching the clinical
        # method of fitting exponential growth between two CT scans.
        t = time.to('day').magnitude
        tumor_cells = species_dict['V_T.C1'].to('cell').magnitude
        n = len(t)
        if n < 2 or tumor_cells[0] <= 0 or tumor_cells[-1] <= 0:
            return np.full_like(t, np.nan) * ureg.day
        dt = t[-1] - t[0]
        if dt <= 0:
            return np.full_like(t, np.nan) * ureg.day
        r = (np.log(tumor_cells[-1]) - np.log(tumor_cells[0])) / dt
        if r <= 0:
            # Tumor shrank or was static; VDT undefined for negative growth
            return np.full_like(t, np.nan) * ureg.day
        vdt = np.log(2.0) / r
        # Broadcast scalar VDT over the time vector for consistent interface
        return np.full_like(t, vdt) * ureg.day
  units: day
  species:
  - V_T.C1
  constants: []
  inputs: []
  support: positive_unbounded
  mapping_rationale: |
    Clinical tumor volume doubling time from CT is computed by fitting exponential growth between two imaging timepoints: VDT = ln(2) / [(ln V2 - ln V1) / (t2 - t1)]. In the model, V_T.C1 (number of cancer cells in the tumor) is proportional to tumor volume under approximately constant cellularity. The observable computes a single overall VDT from the first and last simulation timepoints over the 90-day clinical_progression scenario (untreated post-diagnosis growth), matching the clinical measurement method. A 1-day baseline snapshot is insufficient because transient immune dynamics dominate the short-term growth rate. The scalar VDT is broadcast over the time vector for interface consistency.
  experimental_denominator:
  model_denominator_species:
  unmodeled_denominator_components:
  aggregation:

experimental_context:
  species: human
  system: clinical.biopsy
  mouse_subspecifier:
  indication: PDAC
  treatment:
    history:
    - treatment_naive
    status: off_treatment
    specifier:
  stage:
  cell_lines:
  culture_conditions:
  tissue_source: |
    Primary pancreatic ductal adenocarcinomas in situ in the pancreas, imaged using contrast-enhanced multidetector CT without resection during the natural-history observation window.
  assay_type: |
    Contrast-enhanced multidetector computed tomography (MDCT) with volumetric tumor measurements.

scenario:
  description: |
    Natural-history observation of untreated pancreatic ductal adenocarcinoma with serial multidetector CT imaging prior to any anticancer therapy.
  interventions:
  - intervention_description: |
      No therapeutic intervention (natural disease progression); patients underwent at least two contrast-enhanced multidetector CT scans with no chemotherapy, radiotherapy, or surgery between scans.

empirical_data:
  median:
  - 92.92255692603425
  ci95:
  - - 18.222930104373763
    - 481.6597553258588
  units: day
  sample_size: 100
  sample_size_rationale: |
    One hundred patients with histologically proven pancreatic ductal adenocarcinoma examined with at least two CT scans with no intervening treatment were included (Methods/Abstract).
  index_values:
  index_unit:
  index_type:
  inputs:
  - name: mean_vdt
    value: 132.3
    units: day
    description: |
      Mean tumor volume doubling time (VDT) across 100 untreated pancreatic ductal adenocarcinomas measured on serial multidetector CT scans.
    source_ref: Ahn2017_TVDT
    value_location: Abstract, Results
    value_snippet: |
      Tumor growth rates were -0.4 to 19.9 cm/year (mean, 4.2 +/- 3.8 cm/year) in diameter, and 11.1-13,321.5 cm^3/year (mean, 727.8 +/- 1609.5 cm^3/year) in volume corresponding to VDT of 20.0-976.8 days (mean, 132.3 +/- 132.1 days).
    input_type: direct_parameter
    conversion_formula:
    source_type: text
    figure_id:
    extraction_method:
    extraction_notes:
    dispersion_type:
    dispersion_type_rationale:
  - name: sd_vdt
    value: 132.1
    units: day
    description: |
      Standard deviation of tumor volume doubling time (VDT) across 100 untreated pancreatic ductal adenocarcinomas from serial multidetector CT scans.
    source_ref: Ahn2017_TVDT
    value_location: Abstract, Results
    value_snippet: |
      Tumor growth rates were -0.4 to 19.9 cm/year (mean, 4.2 +/- 3.8 cm/year) in diameter, and 11.1-13,321.5 cm^3/year (mean, 727.8 +/- 1609.5 cm^3/year) in volume corresponding to VDT of 20.0-976.8 days (mean, 132.3 +/- 132.1 days).
    input_type: direct_parameter
    conversion_formula:
    source_type: text
    figure_id:
    extraction_method:
    extraction_notes:
    dispersion_type: sd
    dispersion_type_rationale: |
      The abstract reports 'mean, 132.3 +/- 132.1 days' for VDT. With n=100, interpreting +/-132.1 days as SEM would imply SD = 132.1 * sqrt(100) = 1321 days, inconsistent with the reported VDT range of 20.0-976.8 days. Therefore +/-132.1 days must represent the standard deviation.
  - name: min_vdt
    value: 20.0
    units: day
    description: |
      Minimum observed tumor volume doubling time among 100 untreated pancreatic ductal adenocarcinomas.
    source_ref: Ahn2017_TVDT
    value_location: Abstract, Results
    value_snippet: |
      Tumor growth rates were -0.4 to 19.9 cm/year (mean, 4.2 +/- 3.8 cm/year) in diameter, and 11.1-13,321.5 cm^3/year (mean, 727.8 +/- 1609.5 cm^3/year) in volume corresponding to VDT of 20.0-976.8 days (mean, 132.3 +/- 132.1 days).
    input_type: direct_parameter
    conversion_formula:
    source_type: text
    figure_id:
    extraction_method:
    extraction_notes:
    dispersion_type:
    dispersion_type_rationale:
  - name: max_vdt
    value: 976.8
    units: day
    description: |
      Maximum observed tumor volume doubling time among 100 untreated pancreatic ductal adenocarcinomas.
    source_ref: Ahn2017_TVDT
    value_location: Abstract, Results
    value_snippet: |
      Tumor growth rates were -0.4 to 19.9 cm/year (mean, 4.2 +/- 3.8 cm/year) in diameter, and 11.1-13,321.5 cm^3/year (mean, 727.8 +/- 1609.5 cm^3/year) in volume corresponding to VDT of 20.0-976.8 days (mean, 132.3 +/- 132.1 days).
    input_type: direct_parameter
    conversion_formula:
    source_type: text
    figure_id:
    extraction_method:
    extraction_notes:
    dispersion_type:
    dispersion_type_rationale:
  assumptions:
  - name: n_mc_samples
    value: 100000.0
    units: dimensionless
    description: |
      Number of Monte Carlo samples used to sample a lognormal distribution for tumor volume doubling time based on the reported mean and SD.
    rationale: |
      100,000 samples provide stable (<1%) Monte Carlo error for median and 95% CI estimates of a moderately skewed lognormal distribution.
  distribution_code: |
    def derive_distribution(inputs, ureg):
        import numpy as np
        mean_vdt = inputs['mean_vdt'].to('day')
        sd_vdt = inputs['sd_vdt'].to('day')
        n_mc = int(inputs['n_mc_samples'].magnitude)
        m = mean_vdt.magnitude
        s = sd_vdt.magnitude
        # Convert mean and SD on linear scale to lognormal parameters
        sigma2 = np.log(1.0 + (s**2) / (m**2))
        sigma = np.sqrt(sigma2)
        mu = np.log(m) - sigma2 / 2.0
        rng = np.random.default_rng(42)
        samples = rng.lognormal(mu, sigma, n_mc) * ureg.day
        median_obs = np.median(samples)
        ci95_lower = np.percentile(samples, 2.5)
        ci95_upper = np.percentile(samples, 97.5)
        return {
            'median_obs': median_obs,
            'ci95_lower': ci95_lower,
            'ci95_upper': ci95_upper,
        }

primary_data_source:
  source_tag: Ahn2017_TVDT
  title: |
    Time to Progression of Pancreatic Cancer: Evaluation with Multi-Detector Computed Tomography
  first_author: Ahn
  year: 2016
  doi: 10.1007/s12029-016-9876-7

secondary_data_sources: []

calibration_target_id: tumor_doubling_time

cancer_type: PDAC

tags:
- ai-generated

logfire_trace_id: 019c6ef12b0974dcefc53c7dcb0a688a

extraction_model: gpt-5.1

extraction_reasoning_effort: medium

source_relevance:
